import 'package:flutter/material.dart';

class AppColors { //in order to use colors, we create a class and we should import materialapp package
  static final Color primary = Colors.lightBlue; //final hotreload ile değiştirmez görüntüyü
  static const Color secondary = Color(0xFF80D8FF);

  static const Color textColor = Color(0xFF424242);
  static const Color headingColor = Color(0xFF0277BD);
  static const Color captionColor = Colors.white30;
  static const Color darkButtonTextColor = Colors.white;
  static const Color loginTitleTextColor = Colors.red;
  static const Color textFieldFillColor = Color(0xFFA0A0A0);

}